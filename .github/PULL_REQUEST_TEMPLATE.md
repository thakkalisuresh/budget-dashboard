<!-- PRs target `develop`. `main` is the deploy branch (push → Firebase + Netlify). -->

## What & why

<!-- One or two sentences: what does this change and why. Link any related notes. -->

## Changes

-
-

## Checklist

- [ ] `npm test` passes locally
- [ ] If card reward rates changed, `src/cardRewards.js` and `functions/lib/_card-rewards.mjs` are kept in sync (the `cardRewardsSync` test enforces this)
- [ ] Docs updated if behavior/architecture changed (`README.md`, `CHANGELOG.md`, `docs/`)
- [ ] No secrets, tokens, or `.env*` values committed
- [ ] Verified the change in the app (or noted why it can't be browser-verified)

## Screenshots / notes

<!-- For UI changes, before/after screenshots. For backend, the relevant response or log. -->

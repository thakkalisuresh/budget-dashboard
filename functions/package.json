{
  "name": "functions",
  "type": "module",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "index.js",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "@google-cloud/bigquery": "^8.3.1",
    "@google-cloud/bigquery-storage": "^5.1.2",
    "firebase-admin": "^13.10.0",
    "firebase-functions": "^7.2.5",
    "web-push": "^3.6.7"
  },
  "comments": {
    "overrides": "uuid pinned to >=11.1.1 to clear GHSA-w5hq-g745-h8pq pulled in transitively via firebase-admin (google-gax/gaxios/teeny-request bundle uuid 9.x). firebase-admin@14 (which npm audit suggests) is not yet supported by firebase-functions@7's peer deps (^11||^12||^13), so this surgical override stays. Drop once firebase-functions allows firebase-admin@14."
  },
  "overrides": {
    "uuid": "^11.1.1"
  }
}
